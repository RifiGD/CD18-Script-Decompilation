// ----------------------------------
// RSDK Project: Sonic CD
// Script Description: Fade Screen Object
// Script Author: Christian Whitehead
// Unpacked by Rubberduckycooly's script unpacker
// ----------------------------------

//-------Aliases-------//

private alias 72 : TYPE_FADESCREEN



event ObjectUpdate
	if object.state == 0
		if object.value0 < 320
			object.value0 += 4
		else
			object.state = 1
			arrayPos0 = object.value1
			object[arrayPos0].type = TypeName[Destroyed Mobile]
			object[arrayPos0].priority = 0
			foreach (GROUP_PLAYERS, currentPlayer, ACTIVE_ENTITIES)
				object[currentPlayer].xpos = object[arrayPos0].xpos
			next
			stage.curXBoundary2 = 3840
			arrayPos0++
			ResetObjectEntity(arrayPos0, TypeName[Eggman Escape], 0, object[arrayPos0].xpos, object[arrayPos0].ypos)
		end if
	else
		if object.value0 > 0
			object.value0 -= 4
		else
			object.type = TypeName[Blank Object]
			CreateTempObject(TypeName[Fade Music], 1, object.xpos, object.ypos)
		end if
	end if
	SetScreenFade(224, 224, 224, object.value0)
end event

event RSDKDraw
	DrawSprite(0)
end event

event RSDKLoad
	LoadSpriteSheet("Global/Display.gif")
	SpriteFrame(-16, -16, 32, 32, 1, 143)
end event
